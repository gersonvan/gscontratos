<h3> Editar Ficha</h3>
<div class="navbar navbar-fixed-bottom container">
	<button id="saveButton" class="btn btn-primary glyphicon glyphicon-floppy-disk">Salvar</button>
	<%= link_to 'Ficha', procurement_path(@procurement, :format => 'pdf'), class: 'btn btn-default glyphicon glyphicon glyphicon-list-alt'  %>
	<%= link_to 'Procuração', procurement_path(@procurement, :format => 'pdf2'), class: 'btn btn-default glyphicon glyphicon-file' %>
	<%= link_to 'Contrato', procurement_path(@procurement, :format => 'pdf3'), class: 'btn btn-default glyphicon glyphicon-folder-close'  %>
	<%= link_to 'Voltar', procurements_path, class: 'btn btn-default glyphicon glyphicon-arrow-left' %>
</div>
<%= render 'form' %>